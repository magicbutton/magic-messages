// -------------------------------------------------------------------
// Generated by 365admin-publish
// -------------------------------------------------------------------
/*
---
title: Get  User
---
*/
package cmds

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path"

	"github.com/magicbutton/magic-messages/execution"
	"github.com/magicbutton/magic-messages/schemas"
	"github.com/magicbutton/magic-messages/utils"
)

func UsecasesGetUserPost(ctx context.Context, args []string) (*schemas.User, error) {

	result, pwsherr := execution.ExecutePowerShell("john", "*", "magic-messages", "05-usecases", "10-get-user.ps1", "", "-userid", args[0])
	if pwsherr != nil {
		return nil, pwsherr
	}

	resultingFile := path.Join(utils.WorkDir("magic-messages"), "user.json")
	data, err := os.ReadFile(resultingFile)
	if err != nil {
		return nil, err
	}
	resultObject := schemas.User{}
	err = json.Unmarshal(data, &resultObject)
	if utils.Output == "json" {
		fmt.Println(string(data))
	}
	utils.PrintSkip2FirstAnd2LastLines(string(result))

	return nil, nil

}
